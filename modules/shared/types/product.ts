export interface Product {
    images: string;
    id: string;
    name: string;
    description: string;
}

export interface Carousel {
    images: string;
    productId: string;
    productName: string;
    productDescription: string;
    id: string;
    name: string;
    description: string;
}